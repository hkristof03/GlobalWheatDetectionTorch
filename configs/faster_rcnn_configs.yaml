dataloader:
  path_df: './datasets/train_256x256.csv'
  #path_df: './datasets/train.csv'
  #path_df: './datasets/train_unlabeled.csv'  # use this to add negative images to train
  train_dataset:
    dir_train: './datasets/train_256x256/'
  valid_dataset:
    dir_test: './datasets/test/'
  train_loader:
    batch_size: 16
    shuffle: False
    num_workers: 4
  valid_loader:
    batch_size: 16
    shuffle: False
    num_workers: 4

train:
  epochs: 62
  path_save_model: './artifacts/saved_models/'
  model_name: 'fasterrcnn_256_test'
  path_history: './artifacts/history/'
